{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_VisuAuth": "Microsoft Entra External ID config. Two distinct sections — see Program.cs for why. EntraExternal: app-only Graph CRUD (admin reads/writes the directory). EntraExternal:Web: OIDC sign-in for the End-user UI (customers sign in through the hosted Microsoft page). Populate via `dotnet user-secrets set VisuAuth:EntraExternal:TenantId ...` (and friends) — NEVER commit real values.",
  "VisuAuth": {
    "EntraExternal": {
      "TenantId": "",
      "ClientId": "",
      "ClientSecret": "",
      "TenantDomain": "",
      "AppRoleResourceId": "",
      "Web": {
        "TenantSubdomain": "",
        "TenantId": "",
        "ClientId": "",
        "ClientSecret": "",
        "_ProfileSync": "Opt-in: copy id_token claims onto the Graph user on sign-in. Set Enabled=true and add ClaimToGraphProperty entries (claim type -> Graph user property). given_name->givenName and family_name->surname are always included. Supported targets: givenName, surname, displayName, jobTitle, department, companyName, city, state, country, postalCode, streetAddress.",
        "ProfileSync": {
          "Enabled": false,
          "ClaimToGraphProperty": {}
        }
      }
    }
  }
}
