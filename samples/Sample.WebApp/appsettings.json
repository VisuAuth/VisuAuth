{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_VisuAuth": "Pick the user backend. Default is the ASP.NET Core Identity adapter against the local SQLite file (`identity`). Set to `entra` to flip to the Microsoft Entra ID adapter (Microsoft.Graph + Azure.Identity, app-only auth). When `entra`, fill in VisuAuth:Entra:TenantId / ClientId / ClientSecret — typically via `dotnet user-secrets`. Never commit real values.",
  "VisuAuth": {
    "Backend": "identity",
    "Entra": {
      "TenantId": "",
      "ClientId": "",
      "ClientSecret": "",
      "AppRoleResourceId": ""
    }
  },

  "_ExternalProviders": "Keys for OAuth external-login providers. Each entry is registered conditionally in Program.cs — empty values mean the matching button never appears on /visuauth/login. Fill them in via `dotnet user-secrets set ExternalProviders:Microsoft:ClientId ...` (and the matching :ClientSecret) — never commit real values. Add new sub-objects for Google, Apple, GitHub, etc. and wire them in Program.cs the same way.",
  "ExternalProviders": {
    "Microsoft": {
      "ClientId": "",
      "ClientSecret": ""
    },
    "Google": {
      "ClientId": "",
      "ClientSecret": ""
    },
    "Apple": {
      "ClientId": "",
      "ClientSecret": ""
    },
    "GitHub": {
      "ClientId": "",
      "ClientSecret": ""
    }
  }
}
