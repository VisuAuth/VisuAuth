<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>VisuAuth.EndUserUi</RootNamespace>
    <AssemblyName>VisuAuth.EndUserUi</AssemblyName>
    <AddRazorSupportForMvc>true</AddRazorSupportForMvc>
  </PropertyGroup>

  <PropertyGroup Label="NuGet">
    <PackageId>VisuAuth.EndUserUi</PackageId>
    <Title>VisuAuth End-User Pages</Title>
    <Description>Drop-in end-user authentication pages for VisuAuth: login, register, forgot/reset password, email confirmation, two-factor, profile, and logout. Razor Pages + htmx, themeable via CSS variables, programmatic config, or view override. Multi-tenant aware.</Description>
    <PackageTags>visuauth;login;register;auth;razor;htmx;aspnetcore;identity</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\VisuAuth.Abstractions\VisuAuth.Abstractions.csproj" />
    <!-- The end-user pages already load AdminUi's static assets at runtime
         (/_content/VisuAuth.AdminUi/visuauth.css and visuauth.js). Adding the
         compile-time reference unlocks the shared TagHelpers (va-password,
         va-form-errors) so duplicated markup lives in one source of truth. -->
    <ProjectReference Include="..\VisuAuth.AdminUi\VisuAuth.AdminUi.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
    <PackageReference Include="System.IdentityModel.Tokens.Jwt" />
    <PackageReference Include="QRCoder" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <!-- Same propagation pattern as AdminUi. The end-user UI ships its own
       JSON resource files (login, register, reset, etc.) and needs them
       on disk next to the consumer's binaries. -->
  <ItemGroup>
    <!-- See AdminUi: <Update> instead of <Include> avoids NETSDK1022. -->
    <Content Update="Resources\**\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <Pack>true</Pack>
      <PackagePath>contentFiles\any\any\Resources\;content\Resources\</PackagePath>
      <PackageCopyToOutput>true</PackageCopyToOutput>
    </Content>
  </ItemGroup>

</Project>
