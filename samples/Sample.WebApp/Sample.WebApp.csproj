<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Sample.WebApp</RootNamespace>
    <AssemblyName>Sample.WebApp</AssemblyName>
    <UserSecretsId>visuauth-sample-webapp</UserSecretsId>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\VisuAuth\VisuAuth.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
    <!-- Sample-only: powers the Microsoft external-login button when the
         developer fills in ClientId/ClientSecret. The VisuAuth packages
         themselves ship no provider deps — consumers add the ones they
         actually wire (Google, Apple, etc.). -->
    <PackageReference Include="Microsoft.AspNetCore.Authentication.MicrosoftAccount" />
  </ItemGroup>

</Project>
