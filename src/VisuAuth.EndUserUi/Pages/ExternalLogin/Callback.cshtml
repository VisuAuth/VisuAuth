@page "/visuauth/external-login/callback"
@model VisuAuth.EndUserUi.Pages.ExternalLogin.CallbackModel
@{
    Layout = null;
}
@*
    Empty body — the GET handler always returns a redirect. This page only
    exists as the OAuth landing target the Start handler set as RedirectUri
    on the AuthenticationProperties.
*@
