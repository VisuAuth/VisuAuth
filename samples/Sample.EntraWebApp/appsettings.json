{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_VisuAuth": "Microsoft Entra ID adapter config. Required: TenantId, ClientId, ClientSecret. Optional: AppRoleResourceId (defaults to ClientId), GraphBaseUrl (defaults to public cloud v1.0). Populate via `dotnet user-secrets set VisuAuth:Entra:TenantId ...` (and friends) — NEVER commit real values. See Program.cs for setup steps.",
  "VisuAuth": {
    "Entra": {
      "TenantId": "",
      "ClientId": "",
      "ClientSecret": "",
      "AppRoleResourceId": ""
    }
  }
}
